sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t selvaraj/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true selvaraj/docker-react npm run test
